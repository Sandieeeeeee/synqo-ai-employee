import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {ArrowLeft,ArrowRight,CheckCircle2} from "lucide-react";import InteractiveBackground from "@/components/InteractiveBackground";import {getGuide,guides} from "../content";import styles from "../resources.module.css";
export function generateStaticParams(){return guides.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const guide=getGuide(slug);if(!guide)return{};return{title:guide.title,description:guide.description,alternates:{canonical:`/resources/${slug}`},openGraph:{title:guide.title,description:guide.description,type:"article",url:`https://synqoai.com/resources/${slug}`}}}
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const guide=getGuide(slug);if(!guide)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:guide.title,description:guide.description,author:{"@type":"Organization",name:"Synqo AI"},publisher:{"@type":"Organization",name:"Synqo AI"},mainEntityOfPage:`https://synqoai.com/resources/${guide.slug}`};return <main className={styles.page}><InteractiveBackground/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><nav className="inner-navbar"><Link href="/" className="brand"><span className="brand-mark">S</span><span className="brand-text">SYNQO <strong>AI</strong></span></Link><Link href="/resources" className="inner-back-link"><ArrowLeft size={16}/>Resources</Link></nav><article className={styles.article}><header><small>{guide.category} · {guide.readingTime}</small><h1>{guide.title}</h1><p>{guide.intro}</p></header>{guide.sections.map(section=><section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map(paragraph=><p key={paragraph}>{paragraph}</p>)}{section.points&&<ul>{section.points.map(point=><li key={point}><CheckCircle2/>{point}</li>)}</ul>}</section>)}<section className={styles.faq}><span>FREQUENTLY ASKED QUESTIONS</span>{guide.faq.map(item=><div key={item.question}><h3>{item.question}</h3><p>{item.answer}</p></div>)}</section><footer><h2>See Synqo AI in action.</h2><p>Explore our business AI Employee or open your personal Life Assistant workspace.</p><div className="hero-actions"><Link className="primary-button" href="/how-it-works">How AI Employee works<ArrowRight size={17}/></Link><Link className="secondary-button" href="/life-assistant">Open Life Assistant</Link></div></footer></article></main>}
